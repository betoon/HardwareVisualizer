<Window x:Class="HardwareVisualizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Hardware Visualizer" Height="880" Width="1320"
        MinHeight="720" MinWidth="1100"
        Background="#0f141b"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#e8edf4"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#202a36"/>
            <Setter Property="Foreground" Value="#e8edf4"/>
            <Setter Property="BorderBrush" Value="#344155"/>
            <Setter Property="Padding" Value="10,7"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#243244"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#6ee7f9"/>
                                <Setter Property="Foreground" Value="#ffffff"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1b2532"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#5dade2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#151b24"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#2a3444"/>
                                <Setter Property="Foreground" Value="#667085"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#202a36"/>
            <Setter Property="Foreground" Value="#e8edf4"/>
            <Setter Property="BorderBrush" Value="#344155"/>
            <Setter Property="Padding" Value="7,5"/>
            <Setter Property="Margin" Value="4,0,8,8"/>
            <Setter Property="Width" Value="48"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#202a36"/>
            <Setter Property="Foreground" Value="#e8edf4"/>
            <Setter Property="BorderBrush" Value="#344155"/>
            <Setter Property="Padding" Value="7,5"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
            <Setter Property="Width" Value="98"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"
                                          Focusable="False"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          Foreground="{TemplateBinding Foreground}"
                                          Padding="{TemplateBinding Padding}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="18"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                                      Margin="2,0,4,0"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      TextElement.Foreground="{TemplateBinding Foreground}"/>
                                    <TextBlock Grid.Column="1"
                                               Text="v"
                                               Foreground="#9aa8b8"
                                               FontWeight="Bold"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="#202a36"
                                        BorderBrush="#344155"
                                        BorderThickness="1"
                                        CornerRadius="6"
                                        MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer MaxHeight="260">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ToggleButton" Property="Background" Value="#243244"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#667085"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#202a36"/>
            <Setter Property="Foreground" Value="#e8edf4"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#243244"/>
                                <Setter Property="Foreground" Value="#ffffff"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#2f3b4c"/>
                                <Setter Property="Foreground" Value="#ffffff"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#e8edf4"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="#0f141b"/>
            <Setter Property="BorderBrush" Value="#2f3b4c"/>
        </Style>
        <Style TargetType="{x:Type TabItem}">
            <Setter Property="Foreground" Value="#e8edf4"/>
            <Setter Property="Background" Value="#171d26"/>
            <Setter Property="BorderBrush" Value="#2f3b4c"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabItem}">
                        <Border x:Name="TabBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="7,7,0,0"
                                Margin="0,0,4,0"
                                Padding="{TemplateBinding Padding}"
                                TextElement.Foreground="{TemplateBinding Foreground}">
                            <ContentPresenter x:Name="ContentSite"
                                              ContentSource="Header"
                                              RecognizesAccessKey="True"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#243244"/>
                                <Setter TargetName="TabBorder" Property="BorderBrush" Value="#6ee7f9"/>
                                <Setter Property="Foreground" Value="#ffffff"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBorder" Property="Background" Value="#202a36"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ProgressBar">
            <Setter Property="Height" Value="10"/>
            <Setter Property="Foreground" Value="#5dade2"/>
            <Setter Property="Background" Value="#2a3444"/>
        </Style>
    </Window.Resources>
    <DockPanel Margin="16">
        <Border DockPanel.Dock="Top" Background="#171d26" BorderBrush="#2f3b4c" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Hardware Visualizer" FontSize="28" FontWeight="Bold"/>
                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                        <TextBlock x:Name="StatusText" Text="Starting sensor engine..." Foreground="#9aa8b8"/>
                        <Border x:Name="NetworkBadge" Background="#202a36" BorderBrush="#344155" BorderThickness="1" CornerRadius="8" Padding="8,3" Margin="10,0,0,0">
                            <TextBlock x:Name="NetworkBadgeText" Text="Network: --" FontSize="11" FontWeight="SemiBold" Foreground="#9aa8b8"/>
                        </Border>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                    <WrapPanel HorizontalAlignment="Right">
                        <Button Content="Refresh Now" Click="RefreshNow_Click"/>
                        <Button Content="Save Report" Click="SaveReport_Click"/>
                        <Button Content="Export CSV" Click="ExportCsv_Click"/>
                        <Button Content="Mini Monitor" Click="MiniMonitor_Click"/>
                        <Button Content="Baseline ▾" Click="ToolbarMenu_Click" ToolTip="Capture a baseline and compare it with current readings.">
                            <Button.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Start Baseline" Click="StartBaseline_Click"/>
                                    <MenuItem Header="Compare with Baseline" Click="CompareBaseline_Click"/>
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>
                        <Button Content="Diagnostics ▾" Click="ToolbarMenu_Click" ToolTip="Check sensor setup and compare available data sources.">
                            <Button.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Check Setup" Click="CheckSetup_Click"/>
                                    <MenuItem Header="Direct Sensor Check" Click="DirectCheck_Click"/>
                                    <Separator/>
                                    <MenuItem Header="Compare Sensor Sources" Click="CompareSources_Click"/>
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>
                        <Button Content="Windows Tools ▾" Click="ToolbarMenu_Click" ToolTip="Open useful Windows diagnostic tools.">
                            <Button.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Event Viewer Home" Click="EventViewer_Click"/>
                                    <MenuItem Header="System Event Log" Click="SystemEventLog_Click" ToolTip="Open driver, hardware service, and other Windows system events."/>
                                    <MenuItem Header="Application Event Log" Click="ApplicationEventLog_Click"/>
                                    <MenuItem Header="Reliability Monitor" Click="ReliabilityMonitor_Click" ToolTip="Review crashes, failed updates, and stability history."/>
                                    <Separator/>
                                    <MenuItem Header="Hardware Registry Summary (Read-only)" Click="HardwareRegistrySummary_Click"/>
                                    <MenuItem Header="Open Registry Editor..." Click="RegistryEditor_Click" ToolTip="Shows a safety confirmation before opening the editor."/>
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>
                        <TextBlock x:Name="LastUpdateText" VerticalAlignment="Center" Foreground="#9aa8b8" Margin="0,0,0,8"/>
                    </WrapPanel>
                    <Expander x:Name="SettingsExpander" Header="Settings" IsExpanded="True" Foreground="#e8edf4" HorizontalAlignment="Right">
                        <Border Background="#121922" BorderBrush="#344155" BorderThickness="1" CornerRadius="8" Padding="10,8,2,0" Margin="0,2,0,0">
                            <WrapPanel>
                                <TextBlock Text="Temp Unit" VerticalAlignment="Center" Foreground="#9aa8b8" Margin="0,0,6,8"/>
                                <Button x:Name="CelsiusButton" Content="°C" Width="44" Click="TemperatureUnitButton_Click" ToolTip="Show temperatures in Celsius."/>
                                <Button x:Name="FahrenheitButton" Content="°F" Width="44" Click="TemperatureUnitButton_Click" ToolTip="Show temperatures in Fahrenheit."/>
                                <TextBlock Text="Theme" VerticalAlignment="Center" Foreground="#9aa8b8" Margin="4,0,6,8"/>
                                <ComboBox x:Name="ThemeComboBox" SelectionChanged="ThemeComboBox_SelectionChanged" ToolTip="Changes the dashboard color style.">
                                    <ComboBoxItem Content="Cool" IsSelected="True"/>
                                    <ComboBoxItem Content="Contrast"/>
                                    <ComboBoxItem Content="Thermal"/>
                                </ComboBox>
                                <TextBlock Text="Mode" VerticalAlignment="Center" Foreground="#9aa8b8" Margin="4,0,6,8"/>
                                <ComboBox x:Name="WorkloadComboBox" SelectionChanged="WorkloadComboBox_SelectionChanged" ToolTip="Adjusts Watch/Hot thresholds for the kind of work you are doing.">
                                    <ComboBoxItem Content="Idle" IsSelected="True"/>
                                    <ComboBoxItem Content="Photo"/>
                                    <ComboBoxItem Content="Gaming"/>
                                    <ComboBoxItem Content="Render"/>
                                    <ComboBoxItem Content="Stress"/>
                                </ComboBox>
                                <CheckBox x:Name="QuietModeCheckBox" Content="Quiet" ToolTip="Refreshes more slowly to reduce overhead." Checked="QuietMode_Changed" Unchecked="QuietMode_Changed"/>
                                <CheckBox x:Name="CompactModeCheckBox" Content="Compact" ToolTip="Shows only the main analysis, network dashboard, and overview tabs." Checked="CompactMode_Changed" Unchecked="CompactMode_Changed"/>
                                <TextBlock Text="CPU W/H C" VerticalAlignment="Center" Foreground="#9aa8b8" Margin="4,0,0,8"/>
                                <TextBox x:Name="CpuWatchBox" Text="75" ToolTip="CPU Watch temperature." LostFocus="ThresholdBox_LostFocus"/>
                                <TextBox x:Name="CpuHotBox" Text="90" ToolTip="CPU Hot temperature." LostFocus="ThresholdBox_LostFocus"/>
                                <TextBlock Text="GPU W/H C" VerticalAlignment="Center" Foreground="#9aa8b8" Margin="4,0,0,8"/>
                                <TextBox x:Name="GpuWatchBox" Text="75" ToolTip="GPU Watch temperature." LostFocus="ThresholdBox_LostFocus"/>
                                <TextBox x:Name="GpuHotBox" Text="90" ToolTip="GPU Hot temperature." LostFocus="ThresholdBox_LostFocus"/>
                                <Button Content="Reset" Click="ResetSettings_Click" ToolTip="Restore default theme, mode, and thresholds."/>
                            </WrapPanel>
                        </Border>
                    </Expander>
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer DockPanel.Dock="Top" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Margin="0,0,0,12">
            <StackPanel x:Name="SummaryCards" Orientation="Horizontal"/>
        </ScrollViewer>

        <TabControl x:Name="SensorTabs"/>
    </DockPanel>
</Window>
